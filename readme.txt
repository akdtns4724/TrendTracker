날 읽어라

1. DAO
	1. Mapping 방식
		create(insert) 	: @RequestMapping("/CategoryInsert")
		read				: @RequestMapping("/Category")
		update			: @RequestMapping("/CategoryUpdate")
		delete			: @RequestMapping("/CategoryDelete")
		// Table name + CRUD (단, read일 경우는 Table name만)
		
		
2. 담당
	- 권순명 : Category, Keyword
	- 한지혜 : Log, Major
	- 이유진 : Member, MemberKeyword